# CenTI-R - Variables de entorno
# Copia este archivo a .env y configura tus credenciales

# Base de datos: sqlite (por defecto) o mysql
DB_DRIVER=sqlite

# MySQL (solo si DB_DRIVER=mysql)
# Ejecuta primero: mysql -u root -p < database/schema_mysql.sql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=centir
DB_USER=root
DB_PASS=

# Twilio (Recordatorios WhatsApp)
# Obtén credenciales en https://www.twilio.com/console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
