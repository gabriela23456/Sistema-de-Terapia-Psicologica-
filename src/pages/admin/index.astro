---
/**
 * CenTI-R - Panel de administradores
 * Login para administradores
 */
---
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Administradores - CenTI-R</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
  <div class="layout">
    <header class="header">
      <a href="/" class="brand">
        <span class="brand-name">CenTI-R</span>
        <span class="brand-badge">Admin</span>
      </a>
    </header>

    <main class="main">
      <div class="login-card">
        <h2>Acceso administradores</h2>
        <p class="card-subtitle">Ingresa tus credenciales de administrador</p>
        <p class="card-info">Como administrador podrás ver todas las citas del centro, enviar recordatorios por WhatsApp y confirmar citas con los pacientes.</p>

        <form id="adminForm" class="auth-form">
          <div class="form-group">
            <label for="email">Correo electrónico</label>
            <input type="email" id="email" name="email" placeholder="admin@centir.mx" required />
          </div>
          <div class="form-group">
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password" required />
          </div>
          <div id="alertContainer"></div>
          <button type="submit" class="btn btn-primary">Iniciar sesión</button>
        </form>

        <a href="/" class="back-link">← Volver al inicio</a>
      </div>
    </main>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.getElementById('adminForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const container = document.getElementById('alertContainer');
      container.innerHTML = '';

      const formData = new FormData(e.target);
      const data = {
        action: 'login',
        email: formData.get('email'),
        password: formData.get('password')
      };

      try {
        const API = (typeof window !== 'undefined' && window.location.hostname === 'localhost' && ['4321','4322','4323'].includes(window.location.port || '')) ? 'http://localhost:8080/api' : '/api';
        const res = await fetch(`${API}/auth`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
          credentials: 'include'
        });
        const json = await res.json();

        if (json.success) {
          if (json.user.rol === 'admin') {
            window.location.href = '/admin/panel/';
          } else {
            container.innerHTML = '<div class="alert alert-warning alert-dismissible fade show" role="alert"><strong>Aviso:</strong> No tienes permisos de administrador<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button></div>';
          }
        } else {
          container.innerHTML = '<div class="alert alert-danger alert-dismissible fade show" role="alert"><strong>Error:</strong> ' + (json.error || 'Credenciales incorrectas') + '<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button></div>';
        }
      } catch (err) {
        container.innerHTML = '<div class="alert alert-danger alert-dismissible fade show" role="alert"><strong>Error:</strong> Error de conexión<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button></div>';
      }
    });
  </script>
</body>
</html>

<style is:global>
  :root { --centir-pink: #E91E8C; --centir-purple: #9C27B0; --centir-pink-light: #F8B4D9; }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  html, body { min-height: 100vh; font-family: 'Nunito', system-ui, sans-serif; background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%); color: #fff; display: flex; flex-direction: column; align-items: center; padding: 2rem; }
  .header { width: 100%; max-width: 420px; margin-bottom: 2rem; }
  .brand { display: flex; align-items: center; gap: 0.5rem; color: white; text-decoration: none; }
  .brand-name { font-size: 1.5rem; font-weight: 800; }
  .brand-badge { font-size: 0.75rem; background: var(--centir-pink); padding: 0.2rem 0.5rem; border-radius: 0.25rem; }
  .main { flex: 1; display: flex; align-items: center; }
  .login-card { background: rgba(255,255,255,0.08); backdrop-filter: blur(12px); border-radius: 1rem; padding: 2rem; width: 100%; max-width: 420px; }
  .login-card h2 { margin-bottom: 0.25rem; }
  .card-subtitle { color: var(--centir-pink-light); font-size: 0.9rem; margin-bottom: 0.75rem; }
  .card-info { font-size: 0.85rem; color: rgba(255,255,255,0.8); margin-bottom: 1.5rem; line-height: 1.5; }
  .form-group { margin-bottom: 1rem; }
  .form-group label { display: block; margin-bottom: 0.35rem; font-weight: 600; }
  .form-group input { width: 100%; padding: 0.75rem; border: 1px solid rgba(255,255,255,0.2); border-radius: 0.5rem; background: rgba(255,255,255,0.1); color: #fff; font-size: 1rem; }
  .form-group input::placeholder { color: rgba(255,255,255,0.5); }
  .form-errors { color: #ffb3b3; font-size: 0.9rem; margin-bottom: 1rem; }
  .btn { width: 100%; padding: 0.85rem; border: none; border-radius: 0.5rem; font-size: 1rem; font-weight: 700; font-family: inherit; cursor: pointer; background: linear-gradient(135deg, var(--centir-pink), var(--centir-purple)); color: white; }
  .back-link { display: block; margin-top: 1.5rem; color: var(--centir-pink-light); font-size: 0.9rem; text-align: center; }
</style>
